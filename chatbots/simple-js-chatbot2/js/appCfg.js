var app = {};

app.cfg = {
    /*

    The initial prompt is shown when the page is loaded. A list of categories is then printed.

    */
    INITIAL_PROMPT: 'Hello, which topic are you interested in? (Type "help" to get a list of topics)'
};